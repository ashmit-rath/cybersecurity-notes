# ARP and DHCP

## ARP (Address Resolution Protocol)
- Used to find MAC address from IP address
- Works inside local network

## DHCP (Dynamic Host Configuration Protocol)
- Automatically assigns IP address
- Also assigns:
  - Subnet mask
  - Gateway
  - DNS
